## Describtion
1. Create a automated Raman spectra data processing tools with Qt
2. Add spectra drawing, mapping, intensity calculation, machine learning (PCA)...etc functions.

# how to begin
1. download Qt from website
2. download in Qt：
   ```bash
   pip install -r requirements.txt
